var arr =[];
function arrObj(name,phase,gender) {
 let hasil ={
   name  : name,
   phase : phase,
   gender: gender,
 }
  arr.push(hasil)
}

arrObj('akbar',1,'male')
arrObj('alang',1,'male')
console.log(arr)

function getData(name) {
  
}
